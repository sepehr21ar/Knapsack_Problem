# Knapsack Problem (مسئله کوله‌پشتی)

در این پروژه، مسئله‌ی کوله‌پشتی در دو مدل مختلف بررسی و با زبان **Go** پیاده‌سازی شده است:

1. **کوله‌پشتی صفر و یک (0/1 Knapsack)**  
   حل‌شده با استفاده از **برنامه‌نویسی پویا (Dynamic Programming)**

2. **کوله‌پشتی کسری (Fractional Knapsack)**  
   حل‌شده با استفاده از **الگوریتم حریصانه (Greedy Algorithm)**

هدف، مقایسه‌ی منطق حل، بهینگی، زمان اجرا و نتایج این دو مدل است.

---

## چرا الگوریتم حریصانه برای کوله‌پشتی کسری بهینه است؟

✔️ چون در مدل کسری:

- هر آیتم **قابل تقسیم** است  
- مسئله خاصیت **Divisible** دارد  

🔹 راهبرد الگوریتم حریصانه:

1. آیتم‌ها بر اساس نسبت **Value / Weight** مرتب می‌شوند
2. در هر مرحله آیتمی انتخاب می‌شود که بیشترین ارزش را به ازای هر واحد وزن ایجاد کند

📌 **استدلال شهودی (Intuition):**  
اگر فقط یک واحد وزن آزاد داشته باشیم، بهترین انتخاب آیتمی است که در همان یک واحد، بیشترین سود را بدهد.  
به دلیل قابل تقسیم بودن آیتم‌ها، این انتخاب محلی همیشه منجر به جواب کلی بهینه می‌شود.

---

## چرا الگوریتم حریصانه برای کوله‌پشتی صفر و یک مناسب نیست؟

❌ چون در مدل صفر و یک:

- آیتم‌ها **قابل تقسیم نیستند**
- هر آیتم یا **کاملاً انتخاب می‌شود** یا **اصلاً انتخاب نمی‌شود**

🔹 مشکل اصلی:
تصمیم‌های محلی (Local Optimum) ممکن است باعث از دست رفتن جواب بهینه‌ی کلی شوند.

### مثال:
~~
Capacity = 50

Item A: value = 60 , weight = 10 → ratio = 6
Item B: value = 100, weight = 20 → ratio = 5
Item C: value = 120, weight = 30 → ratio = 4~~


🔸 انتخاب حریصانه:
- A + B → value = 160

🔸 انتخاب بهینه:
- B + C → value = 220

👉 الگوریتم حریصانه با انتخاب زودهنگام A، فرصت جواب بهتر را از دست می‌دهد.

---

## تفاوت اصلی دو مدل مسئله چیست؟

| ویژگی | کوله‌پشتی کسری | کوله‌پشتی صفر و یک |
|------|---------------|-------------------|
| قابلیت تقسیم آیتم | ✅ دارد | ❌ ندارد |
| روش حل بهینه | حریصانه | برنامه‌نویسی پویا |
| تضمین بهینگی Greedy | ✅ | ❌ |
| پیچیدگی زمانی | کمتر | بیشتر |

---

## آیا جواب الگوریتم کسری همیشه ≥ جواب صفر و یک است؟ چرا؟

✅ بله، همیشه **بزرگ‌تر یا مساوی** است.

🔹 دلیل:
- فضای انتخاب در مدل کسری **گسترده‌تر** است
- مدل صفر و یک، یک حالت خاص از مدل کسری محسوب می‌شود

📐 به زبان ریاضی:

> Optimal(Fractional Knapsack) ≥ Optimal(0/1 Knapsack)
> 
> 
برابری فقط زمانی رخ می‌دهد که:
- نیازی به انتخاب کسری نباشد
- همه‌ی آیتم‌ها به‌صورت کامل در کوله‌پشتی جا شوند

---

## مقایسه‌ی زمان اجرای دو الگوریتم

### ⏱ Fractional Knapsack (Greedy)

- مرتب‌سازی آیتم‌ها: `O(n log n)`
- انتخاب آیتم‌ها: `O(n)`

**پیچیدگی کل:**
O(n log n)

---

### ⏱ 0/1 Knapsack (Dynamic Programming)

- ساخت جدول DP با ابعاد `(n × Capacity)`

**پیچیدگی کل:**
O(n × W)


📌 اگر ظرفیت (W) بزرگ باشد، زمان اجرا بسیار افزایش می‌یابد.

---

## تست روی داده‌های بزرگ‌تر

- الگوریتم کسری روی داده‌های بزرگ **بسیار سریع و کارآمد** است
- الگوریتم صفر و یک برای ظرفیت‌های بزرگ **محدودیت عملی** دارد
- به همین دلیل در مسائل صنعتی بزرگ، اغلب از نسخه‌ی کسری یا تقریب‌ها استفاده می‌شود

---

## چه زمانی جواب دو روش برابر می‌شود؟

✔️ در شرایط زیر، جواب دو الگوریتم یکسان است:

1. تمام آیتم‌های انتخاب‌شده در روش کسری، **به‌طور کامل** انتخاب شوند
2. ظرفیت دقیقاً با مجموع وزن آیتم‌ها پر شود
3. هیچ آیتمی به‌صورت کسری انتخاب نشود

### مثال:

```python
Capacity = 10
Items = [
    (value=10, weight=5),
    (value=20, weight=5)
]

Total Value = 30

Fractional Knapsack = 0/1 Knapsack = 30
